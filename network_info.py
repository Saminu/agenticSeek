import socket import subprocess import platform  def get_network_info():     """Get network interface and IP information"""     print("
=== Network Information ===")      # Get hostname and IP address     hostname = socket.gethostname()     ip_address = socket.gethostbyname(hostname)     print(f"Hostname: {hostname}")     print(f"IP Address: {ip_address}")      # Get network interfaces (platform specific)     system = platform.system()     print(f"
=== Network Interfaces ({system}) ===")      if system == "Windows":         subprocess.run(["ipconfig", "/all"], shell=True)     elif system == "Linux" or system == "Darwin":  # Darwin is macOS         subprocess.run(["ifconfig", "-a"], shell=False)     else:         print("Unsupported operating system")  if __name__ == "__main__":     get_network_info()
